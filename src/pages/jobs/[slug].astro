---
// Job detail pages collapsed into the board; the card at /jobs#slug expands
// in place. Old deep links keep working via this redirect.
export const prerender = false;

const { slug } = Astro.params;

return Astro.redirect(`/jobs#${slug}`, 301);
---
